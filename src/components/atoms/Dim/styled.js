import styled from "@emotion/styled";

const Styled = styled.div`
  width: 100%;
  height: 100%;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1000;
  background-color: var(--grayDimmedEffectBlack85D);
`;

export default Styled;
